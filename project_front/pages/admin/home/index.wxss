@import '/styles/common.wxss';

.home {
  min-height: 100vh;
  background: #f7f7f5;
  padding: 32rpx 28rpx 48rpx;
  box-sizing: border-box;
}

.hero {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  background: linear-gradient(135deg, #e04b4b, #f06c6c);
  border-radius: 24rpx;
  padding: 32rpx;
  margin-bottom: 32rpx;
  box-shadow: 0 12rpx 28rpx rgba(224, 75, 75, 0.25);
}

.hero-text {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.hello {
  font-size: 38rpx;
  font-weight: 600;
  color: #fff;
}

.meta {
  font-size: 26rpx;
  color: rgba(255,255,255,0.9);
}

.actions {
  display: flex;
  gap: 12rpx;
}

.actions .mini-btn {
  background: rgba(255,255,255,0.25);
  color: #fff;
  border-radius: 999px;
  font-size: 24rpx;
  padding: 0 20rpx;
}

.actions .mini-btn.logout {
  background: rgba(0,0,0,0.2);
}

.stats-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 18rpx;
  margin-bottom: 32rpx;
}

.stat-card {
  background: #fff;
  border-radius: 20rpx;
  padding: 28rpx 16rpx;
  text-align: center;
  box-shadow: 0 8rpx 20rpx rgba(0, 0, 0, 0.06);
}

.stat-number {
  display: block;
  font-size: 44rpx;
  font-weight: 600;
  color: #e04b4b;
  margin-bottom: 8rpx;
}

.stat-label {
  display: block;
  font-size: 24rpx;
  color: #666;
}

.quick-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 18rpx;
  margin-bottom: 32rpx;
}

.quick-card {
  background: #fff;
  border-radius: 20rpx;
  padding: 22rpx 12rpx;
  text-align: center;
  box-shadow: 0 8rpx 20rpx rgba(0, 0, 0, 0.05);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  transition: transform 0.2s;
}

.quick-card:active {
  transform: scale(0.95);
}

.quick-badge {
  font-size: 48rpx;
  margin-bottom: 4rpx;
}

.quick-card text:last-child {
  font-size: 26rpx;
  color: #444;
}

.section {
  margin-bottom: 32rpx;
}

.section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 18rpx;
}

.section-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
}

.section-link {
  font-size: 26rpx;
  color: #e04b4b;
}

.card {
  background: #fff;
  border-radius: 20rpx;
  padding: 24rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 8rpx 24rpx rgba(0, 0, 0, 0.06);
}

.card:last-child {
  margin-bottom: 0;
}

.card-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #222;
  margin-bottom: 8rpx;
}

.card-meta {
  font-size: 24rpx;
  color: #888;
  margin-bottom: 10rpx;
}

.card-desc {
  font-size: 24rpx;
  color: #666;
  margin-top: 4rpx;
}

.empty {
  padding: 60rpx 32rpx;
  text-align: center;
  color: #999;
  font-size: 26rpx;
  background: #fff;
  border-radius: 20rpx;
  box-shadow: 0 8rpx 20rpx rgba(0, 0, 0, 0.06);
}

.role-stats {
  display: flex;
  justify-content: space-around;
  background: #fff;
  border-radius: 20rpx;
  padding: 32rpx 24rpx;
  box-shadow: 0 8rpx 20rpx rgba(0, 0, 0, 0.06);
}

.role-stat-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12rpx;
}

.role-count {
  font-size: 48rpx;
  font-weight: 600;
  color: #e04b4b;
}

.role-label {
  font-size: 26rpx;
  color: #666;
}
