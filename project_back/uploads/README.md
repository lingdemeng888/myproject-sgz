# uploads 目录说明

此目录用于存储用户上传的文件。

## 目录结构

```
uploads/
├── papers/      # 论文附件（按年/月分组）
│   └── 2025/
│       └── 01/
├── avatars/     # 用户头像
└── temp/        # 临时文件
```

## 注意事项

1. 此目录应添加到 `.gitignore`，不提交到版本控制
2. 生产环境建议使用对象存储（如OSS/MinIO）替代本地存储
3. 定期清理 `temp/` 目录下的临时文件
